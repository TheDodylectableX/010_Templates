//------------------------------------------------
// Vertices
//------------------------------------------------

typedef struct
{
    local uint block_start = FTell();

    uint block_size <bgcolor=CL_SINGLEVALUE>;

    FSeek(block_start + block_size);
} KF2VertexBlock;